import { PagePlaceholder } from "@/components/shell/page-placeholder";

export const metadata = { title: "Zeitkonten · PersonalPlaner" };

export default function AccountsPage() {
  return (
    <PagePlaceholder
      caption="Konten"
      title="Zeitkonten"
      description="Zeitsaldo, Ferien, UEZ und TZT pro Mitarbeitenden — inklusive manueller Buchungen."
      phase="Phase 5"
    />
  );
}
